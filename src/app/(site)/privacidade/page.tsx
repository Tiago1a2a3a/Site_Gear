import { TemporaryPage } from "@shared/components/layout/TemporaryPage";

export default function PrivacidadePage() {
  return (
    <TemporaryPage
      description="A política será elaborada e revisada antes da ativação de qualquer integração que trate dados."
      title="Privacidade"
    />
  );
}
